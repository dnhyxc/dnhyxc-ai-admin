# AI 知识库

本功能域包含 AI 知识库管理相关的实现文档，覆盖 `dnhyxc_ai_db` 业务库中 `knowledge`（主表）与 `knowledge_trash`（回收站表）两张表的后端接入、权限隔离、软删事务与前端双 Tab 管理页。

## 文档列表

- [知识库管理模块](./知识库管理模块.md) — 跨前后端完整实现：双实体设计、`queryList` 泛型抽象、软删事务（先入回收站再删主表）、超管/普通用户权限隔离、前端 `AiKnowledgePage`（Tabs + 查询 + 确认删除）、模块/路由/实体接入
